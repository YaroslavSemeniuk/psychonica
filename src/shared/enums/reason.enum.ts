export enum ReasonEnum {
    USER_NOT_FOUND = 'author not found',
    USER_EXIST = 'user with same name already exist',
    CATEGORY_NOT_FOUND = 'one of the categories was not found',
    CATEGORY_EXIST = 'category with same title already exist',
    ARTICLE_EXIST = 'article with same title already exist',
}
